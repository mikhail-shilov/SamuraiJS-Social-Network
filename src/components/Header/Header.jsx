import React from "react";
import css from "./Header.module.css"

const Header = (props) => {
    return (
        <header className={css.header}  >Это ХЭАДЕР</header>
    );
}

export default Header;